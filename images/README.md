# Screenshot assets

Place official TML screenshots in this directory. Modern `webp` images are preferred for a small download size, with descriptive names such as `diagram-overview.webp`.

When adding a screenshot, replace the corresponding placeholder in `../index.html` with an `img` element. A complete example is included in the gallery comment. Keep `loading="lazy"`, image dimensions, and useful alternative text.
