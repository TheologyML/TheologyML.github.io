# Video assets

The website embeds YouTube videos on demand rather than storing video files locally. Replace the three `data-youtube-id` values in `../index.html` with the IDs from the official TML YouTube URLs.

Videos are loaded only when a visitor presses play, helping the page stay fast and privacy-friendly.
